class Todo {
  String id;
  String taskName;
  bool isCompleted;
  bool isEditEnabled;
  Todo(
      {required this.id,
      required this.taskName,
      required this.isCompleted,
      required this.isEditEnabled});
}
